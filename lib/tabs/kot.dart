import 'package:flutter/material.dart';

class KOT extends StatelessWidget {
  const KOT({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 100,
      width: 200,
      color: Colors.deepPurple,
      child: const Text('1st page'),
    );
  }
}
